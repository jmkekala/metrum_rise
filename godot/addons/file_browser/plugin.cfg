[plugin]

name="File Browser"
description="Injects, standardises, and repairs MANIFEST headers across the project. Expands a bare HEADER marker into a full section divider. Custom fields are declared once and then tracked in every header the tool writes."
author="David Cook"
version="1.0.0"
script="plugin.gd"
